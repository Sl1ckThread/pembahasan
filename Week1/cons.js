const birthYear = 2013;   // Declare a constant variable named 'birthYear' and set it to 2013
console.log(birthYear);   // Output: 2013

// birthYear = 2014;     // This would give an error, because we can’t reassign a const variable

// We can still change items inside a collection like an array
const favoriteFoods = ["pizza", "ice cream"];
console.log(favoriteFoods); // Output: ["pizza", "ice cream"]

favoriteFoods.push("chocolate"); // Add "chocolate" to the array
console.log(favoriteFoods);      // Output: ["pizza", "ice cream", "chocolate"]
